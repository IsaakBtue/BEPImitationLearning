# Claude Chat Context — 2026-05-20

## Session Summary

### What we did

**1. Compared model_3200.pt to current master**
- Model at `logs/rsl_rl/g1_goalkeeper/2026-05-15_22-20-40/model_3200.pt` was trained at git commit `23db4c9`
- Master (`c5fe974`) had added domain randomization, observation history, new reward fixes on top of it
- Key differences documented: DR (encoder_bias, pd_gains, ball_mass, ball_friction, 6-DOF push), obs noise, obs delay (0-2→2-8 steps), ball vanish latency, ball in-flight perturbation, history_length=10, reward formula fixes, catch_success removed, torques_normalized_l2

**2. Created branch `singleMotionNonDomain`**
- Branched from commit `23db4c9` (the exact state model_3200 was trained from)
- `git branch singleMotionNonDomain 23db4c9 && git checkout singleMotionNonDomain`
- Pushed to GitHub: `git push -u origin singleMotionNonDomain`

**3. Fixes applied to `singleMotionNonDomain` (non-DR only)**

*Ball config (`mdp/commands.py`):*
- `sampling_mode`: reverted back to `"start"` (was accidentally "uniform" at 23db4c9; the fix from d163bc8 had been overwritten)
- `_BALL_END_RANGES`: x_min -1.2 → -0.84 (70% of max left extent)
- `t_flight`: min 0.5 → 0.4 s (matches G1's `0.4 + 0.6 * rand`)

*Rewards (`mdp/rewards.py`):*
- `_ball_is_behind`: now uses `_sb_init_vy` delta ≥2.0 m/s (matches G1 exactly), fallback to absolute threshold
- `stopball`: per-step delta (`_sb_prev_vy`) → initial-vy delta (`_sb_init_vy`), threshold 1.0→2.0 (matches G1's `_reward_stopball`)
- `eereach` sigma: 3.0 → 5.0
- `hand_proximity_strict`: returns 2× reward after ball is stopped (`_sb_flag`), matching G1's `(success_flag + 1.0)` formula
- `deviation_waist_joint`: `abs().sum()` → `square().sum()` (matches G1)
- Added `torques_normalized_l2` with per-joint T1 kp map (prevents leg joints dominating over arm joints)

*Env config (`tasks/goalkeeper_env_cfg.py`):*
- Removed `catch_success` reward term
- Removed `catch_success_curriculum`
- `torques` reward now uses `gk_rew.torques_normalized_l2` instead of `mjlab_mdp.joint_torques_l2`
- Added `history_length=10` for both actor and critic obs groups (matches G1's num_actor_history=10)

*PPO config (`tasks/goalkeeper_ppo_cfg.py`):*
- `entropy_coef`: 0.005 → 0.01
- `num_steps_per_env`: kept at 100 (matches model_3200's training; briefly changed to 24 then reverted)
- `max_iterations`: 20,000 → 40,000

**4. What was NOT changed (intentionally)**
- Domain randomization events: no encoder_bias, pd_gains, ball_mass, ball_friction
- Push robot: kept at (10–15 s) interval, 2D velocity only
- Observation delay: kept at 0–2 steps
- No observation noise on proprioceptive terms
- No ball in-flight perturbation
- No ball detection latency (vanish steps)

---

## Current Branch State

| Branch | Commit | Description |
|---|---|---|
| `master` | `c5fe974` | Full DR + all fixes (domain randomization addition) |
| `singleMotionNonDomain` | `23db4c9` + patches | model_3200 baseline + correctness fixes, no DR |

---

## Key File Locations

- Env config: `my_mjlab_project_booster_t1/src/my_mjlab_project_booster_t1/tasks/goalkeeper_env_cfg.py`
- PPO config: `my_mjlab_project_booster_t1/src/my_mjlab_project_booster_t1/tasks/goalkeeper_ppo_cfg.py`
- Rewards: `my_mjlab_project_booster_t1/src/my_mjlab_project_booster_t1/mdp/rewards.py`
- Commands (ball reset): `my_mjlab_project_booster_t1/src/my_mjlab_project_booster_t1/mdp/commands.py`
- Motion data: `my_mjlab_project_booster_t1/src/my_mjlab_project_booster_t1/motions/data/lefthand_t1.npz`
- Checkpoint: `my_mjlab_project_booster_t1/logs/rsl_rl/g1_goalkeeper/2026-05-15_22-20-40/model_3200.pt`

## Train command
```bash
cd /home/isaak/BEPImitationlearning/my_mjlab_project_booster_t1
.venv/bin/python -m mjlab.scripts.train goalkeeper_booster_t1 --gpu-ids '[0]' --num-steps-per-env 100
```

## Play command
```bash
cd /home/isaak/BEPImitationlearning/my_mjlab_project_booster_t1
.venv/bin/python -m mjlab.scripts.play goalkeeper_booster_t1_withoverlay --checkpoint-file logs/rsl_rl/g1_goalkeeper/2026-05-15_22-20-40/model_3200.pt --motion-file src/my_mjlab_project_booster_t1/motions/data/lefthand_t1.npz
```
