You are doing a deep third-pass code audit of the Booster T1 AMP goalkeeper port.
Two previous audit rounds already found and fixed many issues. This round should
verify those fixes are correct AND find anything still missing.

## Coordinate system note (critical context)
G1 original: ball approaches from +X, robot faces +X, lateral axis = Y.
T1 port: ball approaches from +Y, robot faces +Y, lateral axis = X.
This 90° rotation was applied in convert_booster.py on the motion npz files.
Every reward/obs that references an axis must use the rotated equivalent.
Check for any axis errors throughout.

## What was already fixed (verify these are correct, not just present)
1. Ball obs masking: catchstep (counts down from 50), startstep (per-env 50-randint(3,11)),
   vanish_step (per-env randint(0,30)), flying flag, step-cache to avoid double-call bug.
   Files: mdp/observations.py (_compute_ball_visibility), mdp/commands.py (_reset_ball)

2. jump_scale in eereach: motion_type_ids 2/3 = jump envs, jump_scale = 3+3*curriculumupdate,
   curriculumupdate = _ball_difficulty*2, post-pass vel_sigma = flat 2.0 (not *2.0).
   File: mdp/rewards.py (eereach function)

3. successland: air_reward removed (no jump-region gate in T1).
   File: mdp/rewards.py (successland function)

4. Curriculum: 6 terms wired up (stopball, eereach, hand_proximity_strict,
   ball_difficulty, dof_pos_limits, torque_limits).
   File: tasks/goalkeeper_amp_env_cfg.py

5. DR removed: base_com, encoder_bias, foot_friction, push_robot popped from cfg.events.
   File: tasks/goalkeeper_amp_env_cfg.py

6. Ball spawn in play: x_start narrowed to (-0.2, 0.2), x_end to (-0.6, 0.6).
   File: mdp/resets.py (_shoot_ball)

## Source files to read IN FULL (G1 originals)
- /home/robocup/IsaakB/BEPImitationLearning/Humanoid-Goalkeeper/legged_gym/legged_gym/envs/g1/g1_29_env.py
- /home/robocup/IsaakB/BEPImitationLearning/Humanoid-Goalkeeper/legged_gym/legged_gym/envs/g1/g1_29_config.py
- /home/robocup/IsaakB/BEPImitationLearning/Humanoid-Goalkeeper/legged_gym/legged_gym/envs/base/legged_robot.py

## Port files to read IN FULL
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/mdp/rewards.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/mdp/observations.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/mdp/commands.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/mdp/resets.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/tasks/goalkeeper_amp_env_cfg.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/rsl_rl_amp/runners/him_amp_runner.py
- /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster/src/imitationlearningbooster/tasks/goalkeeper_amp_ppo_cfg.py

## For each item found, report:
### [Name]
**G1**: exact code/values (file:line)
**Port status**: CORRECT / WRONG (describe error) / MISSING
**Axis check**: correct axis used? (X vs Y adapted correctly?)
**Fix**: exact copy-paste code if wrong/missing, or "None" if correct

## Specific things to verify carefully
- flying flag: G1 uses ball_vx > initial_vx + threshold; port uses ball_vy — correct?
- catchstep decrement: happens in _update_command every step — confirmed?
- _ball_end_target: set per-env in _reset_ball, used by eereach phase1 — correct?
- stayonline: uses root_pos[:, 1] not [:, 0] — axis correct for +Y approach?
- noretreat: which axis/frame is forward? body frame [:, 0] or world Y?
- eereach phase1 (pre-positioning): asidegoal uses end_target_local[:, 0] (X lateral) — correct given 90° rotation?
- stopball: uses ball_vy delta not ball_vx — correct for +Y approach?
- _ball_is_behind: uses ball_y_local < 0 not ball_x — correct?
- AMP discriminator obs (joint_pos_amp): what does it return? Is it consistent with training?
- observation history: 10 frames stacked — confirmed in play cfg too?
- Does the play cfg (goalkeeper_amp_play_env_cfg) correctly inherit all fixes?
- Are there any events still present in play mode that should be removed?
- PPO hyperparameters: compare G1 legged_gym PPO cfg vs goalkeeper_amp_ppo_cfg.py (lr, entropy, clip, num_steps, mini_batches)
- AMP discriminator: how many layers, hidden size, learning rate — does it match G1?
- motion data: are all 6 npz files present and correctly converted (check convert_all.py output)?
