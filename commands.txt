# === SimpleGoalKeeper — NPZ already regenerated, run these to train ===

# Re-convert motions (already done, only re-run if PKL files change):
cd /home/isaak/BEPImitationlearning/SimpleGoalKeeper
uv run sgk_convert --input-dir Motions --output-dir src/simple_goalkeeper/motions/data

# Train (local):
cd /home/isaak/BEPImitationlearning/SimpleGoalKeeper
uv run sgk_train Mjlab-BeyondAMP-Goalkeeper-T1 --num-envs 4096

# Play (zero policy sanity check):
cd /home/isaak/BEPImitationlearning/SimpleGoalKeeper
uv run sgk_play Mjlab-BeyondAMP-Goalkeeper-T1 --agent zero --num-envs 1
