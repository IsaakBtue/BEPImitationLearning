# ⚠️ All existing checkpoints have 87-dim obs, current config has 142-dim.
# Existing checkpoints CANNOT be loaded into the current config.
# Must train a new model.

# Train (64 envs, smoke test — verifies config works)
CUDA_VISIBLE_DEVICES=0 uv run python -m mjlab.scripts.train goalkeeper_booster_t1 --env.scene.num-envs 64

# Train (full 1020 envs)
cd /home/isaak/BEPImitationlearning/my_mjlab_project_booster_t1 && uv run python -m mjlab.scripts.train goalkeeper_booster_t1 --env.scene.num-envs 1020

# Play (once a new checkpoint exists from the training above)
cd /home/isaak/BEPImitationlearning/my_mjlab_project_booster_t1 && uv run python -m mjlab.scripts.play goalkeeper_booster_t1_withoverlay \
  --checkpoint-file logs/rsl_rl/g1_goalkeeper/<NEW_RUN_TIMESTAMP>/model_200.pt \
  --motion-file src/my_mjlab_project_booster_t1/motions/data/lefthand_t1.npz

# Reconvert motion data if ever needed
cd /home/isaak/BEPImitationlearning/my_mjlab_project_booster_t1 && uv run python src/my_mjlab_project_booster_t1/motions/convert_booster.py
