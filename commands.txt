# Install missing LaTeX packages needed to compile the tex file:
sudo apt-get install -y texlive-latex-recommended texlive-latex-extra

# Then compile (run twice for TOC/cross-refs):
cd /home/isaak/BEPImitationlearning/zzzzfixes
pdflatex booster_t1_port_analysis.tex
pdflatex booster_t1_port_analysis.tex

# Clean up aux files after:
rm -f booster_t1_port_analysis.{aux,log,toc,out}
