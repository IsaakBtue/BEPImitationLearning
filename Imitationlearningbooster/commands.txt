# Retrain from scratch (18th-pass: curriculum fix + smoothness loss fix + new knee dataset):
cd /home/robocup/IsaakB/BEPImitationLearning/Imitationlearningbooster && env -u PYTHONPATH .venv/bin/train goalkeeper_booster_t1_amp
